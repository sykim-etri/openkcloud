# ============================================================
# runtimeclass.yaml: NVIDIA device-plugin 및 GPU Pod용 RuntimeClass
# 생성일: 2026-04-22
# ============================================================
{{- if .Values.runtimeClass.nvidia.enabled }}
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
{{- end }}
