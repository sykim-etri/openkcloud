apiVersion: v1
kind: Service
metadata:
  name: kcloud-infra
  labels:
    app: kcloud-infra
    component: infrastructure
spec:
  type: ClusterIP
  selector:
    app: kcloud-infra
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  sessionAffinity: None
