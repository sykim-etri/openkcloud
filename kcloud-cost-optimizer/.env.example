# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=policy_db
POSTGRES_USER=policy_user
POSTGRES_PASSWORD=your-secure-postgres-password-here

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your-secure-redis-password-here

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=your-secure-grafana-password-here

# MinIO Configuration (if used)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=your-secure-minio-password-here

# API Configuration
API_TOKEN=your-api-token-here
WEBHOOK_TOKEN=your-webhook-token-here

# IMPORTANT:
# 1. Copy this file to .env
# 2. Replace all placeholder values with strong, randomly generated passwords
# 3. NEVER commit the .env file to version control
# 4. Use at least 16 characters for passwords with mixed case, numbers, and special characters
